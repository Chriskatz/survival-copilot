{
  "name": "survival-copilot",
  "version": "0.1.0",
  "description": "Off-grid wilderness AI assistant — Meshtastic LoRa mesh + QVAC local LLM. QVAC Hackathon I entry.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@qvac/sdk": "latest"
  },
  "devDependencies": {
    "@qvac/cli": "latest",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
